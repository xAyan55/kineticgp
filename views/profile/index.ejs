<%- include('../layouts/header', { title: 'Profile Settings' }) %>

<div class="min-h-screen bg-[#101213] flex flex-col lg:flex-row">
  <!-- Sidebar Component -->
  <%- include('../components/sidebar') %>

  <!-- Main Content Area -->
  <main class="flex-1 lg:pl-56 pt-16 lg:pt-0 pb-20 lg:pb-10 min-h-screen flex flex-col">
    <!-- Top Bar Header -->
    <header class="h-16 px-6 lg:px-10 border-b border-white/5 flex items-center justify-between bg-[#101213]/80 backdrop-blur-xl sticky top-0 z-20">
      <div class="flex items-center gap-2 text-sm text-neutral-400">
        <a href="/dashboard" class="hover:text-white transition">Panel</a>
        <span>/</span>
        <span class="text-[#1873d3] font-medium">Profile Settings</span>
      </div>

      <a href="/dashboard" class="btn-secondary text-xs sm:text-sm py-1.5 px-3">
        Back to Dashboard
      </a>
    </header>

    <div class="p-6 lg:p-10 max-w-[1400px] w-full mx-auto space-y-8 flex-1">

      <!-- PROFILE HEADER BANNER -->
      <div class="kgp-card overflow-hidden p-0 relative">
        <div class="h-44 sm:h-56 w-full bg-cover bg-center relative" style="background-image: url('<%= user.banner %>');">
          <div class="absolute inset-0 bg-gradient-to-t from-[#1a1c1e] via-transparent to-black/30"></div>
        </div>

        <div class="px-6 sm:px-10 pb-8 pt-0 relative flex flex-col sm:flex-row items-start sm:items-end justify-between gap-6 -mt-16 sm:-mt-20">
          <div class="flex flex-col sm:flex-row items-start sm:items-end gap-6">
            <img id="currentAvatarPreview" src="<%= user.avatar %>" alt="User avatar" class="w-24 h-24 sm:w-32 sm:h-32 rounded-3xl border-4 border-[#1a1c1e] object-cover shadow-soft-2xl bg-[#101213]">
            <div class="mb-2">
              <h1 class="text-2xl sm:text-3xl font-extrabold text-white"><%= user.username %></h1>
              <p class="text-neutral-400 text-sm"><%= user.email %></p>
            </div>
          </div>

          <div class="mb-2 flex items-center gap-2">
            <span class="px-3 py-1 rounded-full bg-[#1873d3]/20 border border-[#1873d3]/40 text-[#1873d3] text-xs font-semibold uppercase">
              Administrator
            </span>
          </div>
        </div>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="px-4 py-3 rounded-xl bg-red-500/10 border border-red-500/30 text-red-400 text-sm font-medium">
          <% if (error === 'invalid_username') { %>Username must be at least 3 characters.
          <% } else if (error === 'username_taken') { %>That username is already taken.
          <% } else if (error === 'wrong_current_password') { %>Incorrect current password.
          <% } else if (error === 'short_password') { %>New password must be at least 6 characters.
          <% } else if (error === 'password_mismatch') { %>New passwords do not match.
          <% } else { %>Update failed. Please check your inputs.<% } %>
        </div>
      <% } %>

      <% if (typeof success !== 'undefined' && success) { %>
        <div class="px-4 py-3 rounded-xl bg-emerald-500/10 border border-emerald-500/30 text-emerald-400 text-sm font-medium">
          <% if (success === 'username_updated') { %>Username updated successfully!
          <% } else if (success === 'password_updated') { %>Password changed successfully!
          <% } else if (success === 'avatar_updated') { %>Profile avatar updated successfully!<% } %>
        </div>
      <% } %>

      <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
        
        <!-- LEFT COLUMN: AVATAR & ACCOUNT INFORMATION -->
        <div class="lg:col-span-5 space-y-8">
          
          <!-- AVATAR SELECTION CARD -->
          <div class="kgp-card p-6 space-y-6">
            <div>
              <h2 class="text-lg font-bold text-white">Avatar Selection</h2>
              <p class="text-xs text-neutral-400 mt-1">Choose between Minecraft Steve, Alex, or a custom avatar URL.</p>
            </div>

            <form action="/dashboard/profile/update-avatar" method="POST" class="space-y-6">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <input type="hidden" id="selectedAvatarInput" name="avatar" value="<%= user.avatar %>">

              <div>
                <label class="block text-xs font-semibold uppercase tracking-wider text-neutral-300 mb-3">Default Minecraft Avatars</label>
                <div class="grid grid-cols-2 gap-4">
                  <!-- Steve Avatar Button -->
                  <button type="button" class="avatar-option-btn flex flex-col items-center gap-2 p-4 rounded-xl bg-[#101213] border border-white/10 hover:border-[#1873d3] transition text-center group <%= user.avatar === '/images/banners/big-steve-face.png' ? 'border-[#1873d3] ring-2 ring-[#1873d3]' : '' %>" data-avatar-url="/images/banners/big-steve-face.png">
                    <img src="/images/banners/big-steve-face.png" alt="Steve Face" class="w-14 h-14 rounded-xl object-cover group-hover:scale-105 transition">
                    <span class="text-xs font-semibold text-white">Steve</span>
                  </button>

                  <!-- Alex Avatar Button -->
                  <button type="button" class="avatar-option-btn flex flex-col items-center gap-2 p-4 rounded-xl bg-[#101213] border border-white/10 hover:border-[#1873d3] transition text-center group <%= user.avatar === '/images/banners/big-alex-face.png' ? 'border-[#1873d3] ring-2 ring-[#1873d3]' : '' %>" data-avatar-url="/images/banners/big-alex-face.png">
                    <img src="/images/banners/big-alex-face.png" alt="Alex Face" class="w-14 h-14 rounded-xl object-cover group-hover:scale-105 transition">
                    <span class="text-xs font-semibold text-white">Alex</span>
                  </button>
                </div>
              </div>

              <button type="submit" class="w-full btn-kinetic py-2.5 text-sm justify-center">
                Save Avatar
              </button>
            </form>
          </div>

          <!-- ACCOUNT INFO CARD -->
          <div class="kgp-card p-6 space-y-4">
            <h2 class="text-lg font-bold text-white border-b border-white/5 pb-3">Account Details</h2>
            <div class="space-y-3 text-xs">
              <div class="flex justify-between py-1 border-b border-white/5">
                <span class="text-neutral-500">Account ID:</span>
                <span class="font-mono text-white font-semibold">#<%= user.id %></span>
              </div>
              <div class="flex justify-between py-1 border-b border-white/5">
                <span class="text-neutral-500">Email Address:</span>
                <span class="text-neutral-300 font-medium"><%= user.email %></span>
              </div>
              <div class="flex justify-between py-1 border-b border-white/5">
                <span class="text-neutral-500">Role & Privileges:</span>
                <span class="text-emerald-400 font-semibold uppercase">Administrator</span>
              </div>
              <div class="flex justify-between py-1">
                <span class="text-neutral-500">Account Created:</span>
                <span class="text-neutral-300 font-medium"><%= user.created_at %></span>
              </div>
            </div>
          </div>

        </div>

        <!-- RIGHT COLUMN: USERNAME & PASSWORD EDIT FORMS -->
        <div class="lg:col-span-7 space-y-8">
          
          <!-- EDIT USERNAME -->
          <div class="kgp-card p-6 sm:p-8 space-y-6">
            <div>
              <h2 class="text-xl font-bold text-white">Edit Username</h2>
              <p class="text-xs text-neutral-400 mt-1">Update your display username used across the panel and activity logs.</p>
            </div>

            <form action="/dashboard/profile/update-username" method="POST" class="space-y-4">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">

              <div>
                <label for="username" class="block text-xs font-semibold uppercase tracking-wider text-neutral-300 mb-1.5">New Username</label>
                <input id="username" name="username" type="text" value="<%= user.username %>" required class="w-full px-4 py-2.5 rounded-xl bg-[#101213] border border-white/10 text-white text-sm focus:border-[#1873d3] focus:ring-1 focus:ring-[#1873d3] transition">
              </div>

              <div class="pt-2 flex justify-end">
                <button type="submit" class="btn-kinetic text-sm py-2.5 px-6">
                  Update Username
                </button>
              </div>
            </form>
          </div>

          <!-- CHANGE PASSWORD -->
          <div class="kgp-card p-6 sm:p-8 space-y-6">
            <div>
              <h2 class="text-xl font-bold text-white">Security & Password</h2>
              <p class="text-xs text-neutral-400 mt-1">Ensure your account uses a strong, unique password.</p>
            </div>

            <form action="/dashboard/profile/update-password" method="POST" class="space-y-4">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">

              <div>
                <label for="current_password" class="block text-xs font-semibold uppercase tracking-wider text-neutral-300 mb-1.5">Current Password</label>
                <input id="current_password" name="current_password" type="password" required placeholder="••••••••" class="w-full px-4 py-2.5 rounded-xl bg-[#101213] border border-white/10 text-white text-sm focus:border-[#1873d3] focus:ring-1 focus:ring-[#1873d3] transition">
              </div>

              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div>
                  <label for="new_password" class="block text-xs font-semibold uppercase tracking-wider text-neutral-300 mb-1.5">New Password</label>
                  <input id="new_password" name="new_password" type="password" required placeholder="••••••••" class="w-full px-4 py-2.5 rounded-xl bg-[#101213] border border-white/10 text-white text-sm focus:border-[#1873d3] focus:ring-1 focus:ring-[#1873d3] transition">
                </div>

                <div>
                  <label for="confirm_password" class="block text-xs font-semibold uppercase tracking-wider text-neutral-300 mb-1.5">Confirm New Password</label>
                  <input id="confirm_password" name="confirm_password" type="password" required placeholder="••••••••" class="w-full px-4 py-2.5 rounded-xl bg-[#101213] border border-white/10 text-white text-sm focus:border-[#1873d3] focus:ring-1 focus:ring-[#1873d3] transition">
                </div>
              </div>

              <div class="pt-2 flex justify-end">
                <button type="submit" class="btn-kinetic text-sm py-2.5 px-6">
                  Change Password
                </button>
              </div>
            </form>
          </div>

        </div>

      </div>

    </div>
  </main>
</div>

<%- include('../layouts/footer', { script: 'profile' }) %>
